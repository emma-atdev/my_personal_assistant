"""Notion 저장용 마크다운 출력 포맷 — 브리핑, 리포트, changelog 공통 가이드.

크론 서브에이전트와 크론잡이 Notion에 저장할 콘텐츠를 생성할 때 이 포맷을 따른다.
`_append_markdown`가 파싱할 수 있는 마크다운 문법만 사용한다.
"""

# ── 공통 마크다운 가이드 (LLM 시스템 프롬프트용) ──────────────

MARKDOWN_GUIDE = """\
Notion 저장용 마크다운 작성 규칙 (반드시 준수):

지원되는 블록 문법:
- `## 제목` / `### 소제목` (# 대제목은 페이지 제목과 중복되므로 본문에서 사용 금지)
- `- 항목` (불릿 리스트)
- `1. 항목` (번호 리스트)
- `- [ ] 미완료` / `- [x] 완료` (체크박스)
- ``` (코드 블록 — 언어 태그 권장)
- `> 인용문`
- `> [!NOTE]` / `> [!TIP]` / `> [!WARNING]` (콜아웃)
- `---` (구분선)

지원되는 인라인 문법:
- `**볼드**` — 핵심 키워드 강조
- `*이탤릭*` — 부가 설명, 출처
- `` `코드` `` — 함수명, 변수명, 명령어
- `[텍스트](URL)` — 링크

작성 원칙:
- 핵심 정보를 먼저, 부가 설명은 뒤에
- 한 문단은 2~3줄 이내
- 리스트는 7개 이내로 (많으면 하위 섹션으로 분리)
- 논문/뉴스 항목은 반드시 제목 + 링크 + 한 줄 요약 형식
- 코드 예시가 있으면 코드 블록 사용
- 중요 안내사항은 콜아웃 사용
"""

# ── 아침 브리핑 포맷 ─────────────────────────────────────────

BRIEFING_TEMPLATE = """\
## 📰 오늘의 AI 브리핑

> [!NOTE]
> {date} 기준 수집된 논문과 뉴스 요약입니다.

## Hugging Face 인기 논문

{hf_papers}

---

## ArXiv 최신 LLM 논문

{arxiv_papers}

---

## AI 뉴스

{ai_news}

---

*{date} 자동 생성*
"""

BRIEFING_INSTRUCTIONS = """\
아침 브리핑 작성 시 다음 형식을 따르세요:

각 논문/뉴스 항목:
1. **[논문 제목](링크)** — 한 줄 요약 (불릿 리스트)
2. 핵심 기여나 의미가 있으면 바로 아래에 부가 설명 1줄

예시:
- **[논문 제목](https://arxiv.org/abs/...)** — Transformer 아키텍처를 개선해 추론 속도 2배 향상
  *Meta AI, 2026*
"""

# ── 주간 리포트 포맷 ─────────────────────────────────────────

REPORT_TEMPLATE = """\
## 📊 주간 리포트

> [!NOTE]
> {week_label} 주차 활동 요약입니다.

## API 비용 현황

{cost_summary}

---

## 이번 주 주요 활동

{activities}

---

*{date} 자동 생성*
"""

REPORT_INSTRUCTIONS = """\
주간 리포트 작성 시 다음 형식을 따르세요:

비용 현황:
- 모델별 사용량을 번호 리스트로 정리
- 총 비용은 **볼드**로 강조

주요 활동:
- 완료된 작업은 `- [x] 항목` 체크박스
- 진행 중인 작업은 `- [ ] 항목`
- 각 항목에 간단한 설명 포함
"""

# ── Changelog 포맷 ───────────────────────────────────────────

CHANGELOG_INSTRUCTIONS = """\
Changelog 항목 작성 시 다음 형식을 따르세요:

### {date}

- **[카테고리]** 변경 내용 요약 — `관련 파일/도구명`

카테고리: 기능 추가, 버그 수정, 개선, 설정 변경, 배포
"""
